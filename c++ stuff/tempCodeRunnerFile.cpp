#include <iostream>
#include <cmath>

int main() {

    int x;
    int y;
    int z;
    std::cin >> x;
    std::cin >> y;
    z = sqrt(pow(x,2) + pow(y,2));
    std::cout << z;

    return 0;
}